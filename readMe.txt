pip install pyttsx3
pip install speechrecognition
pip install pipwin
pipwin install pyaudio
python -m pip install nltk==3.5
	install nltk lib punkt-> nltk.download(punkt),
	nltk.download('stopwords')

python -m pip install numpy matplotlib
